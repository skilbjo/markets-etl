machine:
  services:
    - docker
  environment:
    DOCKER_REMOTE: quay.io/skilbjo/markets-etl

test:
  override:
    - lein cljfmt check
    - lein test

deployment:
  quay:
    branch: /.*/
    commands:
      - deploy/bin/cpu-arch-publish-image
