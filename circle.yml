machine:
  services:
    - docker
    - postgresql

test:
  override:
    - lein cljfmt check
    - lein test

deployment:
  quay:
    branch: /.*/
    commands:
      - deploy/bin/cpu-arch-publish-image
